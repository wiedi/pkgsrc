$NetBSD$

Fix Python libdir under MULTIARCH.

--- hg.orig	2016-09-01 19:01:36.000000000 +0000
+++ hg
@@ -17,7 +17,10 @@ if os.environ.get('HGUNICODEPEDANTRY', F
     except NameError:
         pass
 
-libdir = '@LIBDIR@'
+if os.environ.get('ABI') == '64':
+	libdir = '@LIBDIR64@'
+else:
+	libdir = '@LIBDIR32@'
 
 if libdir != '@' 'LIBDIR' '@':
     if not os.path.isabs(libdir):
